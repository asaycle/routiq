version: v2

modules:
  - path: api/proto

deps:
  - buf.build/googleapis/googleapis

lint:
  # デファクトのルールセット。AIP系を含む標準セットでまずは十分。
  use:
    - DEFAULT
  ignore:
    - api/proto/vendor

breaking:
  # 既存ファイル単位の後方互換チェック（導入初期は FILE 推奨）
  use:
    - FILE
